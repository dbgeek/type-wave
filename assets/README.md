# Assets

Media referenced by the root [README](../README.md).

- **`type-wave-demo.gif`** _(not yet committed — drop the file here)_ — a short
  (~5-10 s) demo: hold the Talk Key, speak, release, and the text lands at the cursor,
  with the HUD visible. Recorded on an Apple Silicon Mac. Keep it small (aim for
  < ~5 MB) so the README stays light. The README's top-of-page `<img>` renders it once
  the file exists on the branch.
