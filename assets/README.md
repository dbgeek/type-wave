# Assets

Media referenced by the root [README](../README.md).

- **`type-wave-demo.gif`** — a ~10 s demo against the live daemon: hold the Talk Key,
  speak, release, and the Final Transcript lands at the cursor of a TextEdit window,
  with the HUD waveform/processing pill visible. Recorded on an Apple Silicon Mac
  (960 px wide, 12 fps, ~0.5 MB). Re-record with the same shape if the HUD or flow
  changes; keep it under ~5 MB so the README stays light.
